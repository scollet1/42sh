#include "../incl/42sh.h"

t_env	*cd(t_env *env, const char *path)
{
	(void)path;
	/*
	 * check if path exists
	 * if true:
	 * 		cd to dir
	 * else:
	 * 		set error and print to user
	 * 	return env
	 */
	return (env);
}
